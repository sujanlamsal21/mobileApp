import React from "react";
import {Center,Text} from "native-base"
function Box(props){
    return(
        <Center size={val} bg={props.bgColor} rounded={props.rounded} _text={props.styles} shadow={"3"}>
          <Text fontSize="lg" color="red.100">Present Days</Text>
            3
          </Center>
    )
}